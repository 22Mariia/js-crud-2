{{!
Це код container файлу для index сторінки,
в якому міститься код, котрий буде віддаватися по шляху (PATH) - "/"
}}

<main>

  <section>
    <div class="form">
      <h1 class="form__title">Список товарів</h1>
      <div class="products-list">
        {{#with data.products}}
        {{#if isEmpty}}
        <mark>Список товарів пуст</mark>
        {{else}}

        {{#each list}}
        <div class="form__content">
          <div class="form__subtitle">{{name}}</div>
          <div>{{description}}</div>
          <div class="id">ID: {{id}}</div>
          <div class="form__content--main">
            <div class="form__subtitle">{{price}}₴</div>
            <a href="/product-edit?id={{id}}" class="link">Редагувати</a>
          </div>

        </div>



        {{/each}}

        {{/if}}
        {{/with}}

      </div>
      <a href="/product-create" class="form__button">Додати новий товар</a>
    </div>
  </section>


</main>


{{!
Цей код містить лише те, що буде виводитись в

<body>
  Адже наш layout/default.hbs має вже структуру HTML
  }}